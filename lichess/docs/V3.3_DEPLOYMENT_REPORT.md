# C0BR4 v3.3 GCP Deployment - November 27, 2025

## Deployment Summary

Successfully deployed **C0BR4 v3.3** with adaptive time management to the Lichess bot on Google Cloud Platform.

### Deployment Details

- **GCP Project**: `c0br4-lichess-bot` (Project #643483548191)
- **VM Instance**: `c0br4-production-bot`
- **Zone**: `us-central1-a`
- **Machine Type**: `e2-small`
- **Container**: `c0br4-production-v31` (Docker)
- **Engine Version**: **C0BR4 v3.3** (adaptive depth 6-10)

### Files Deployed

1. **Linux Binary**: `/home/c0br4/engines/C0BR4_v3.3/C0BR4_v3.3` (62.8 MB)
2. **Configuration**: `/home/c0br4/config.yml` (updated for v3.3)

### Architecture

The bot runs in a Docker container with the following mounts:
```
/home/c0br4/engines    → /lichess-bot/engines (engine binaries)
/home/c0br4/config.yml → /lichess-bot/config.yml (configuration)
/home/c0br4/logs       → /lichess-bot/logs (log files)
```

Container: `ghcr.io/lichess-bot-devs/lichess-bot:latest`

### v3.3 Features Deployed

#### Adaptive Search Depth
- **Classical (30min)**: Target depth **9-10**
- **Rapid (10min)**: Target depth **8**
- **Blitz (3min)**: Target depth **6**
- **Bullet (30s)**: Target depth **4**

#### Conservative Time Management
- Saves 50+ moves worth of time in longer games
- Uses 75% threshold before attempting new depth
- Position-aware: Fast in simple endgames, thorough in complex middlegames

#### Expected Performance Improvements
- **+150-200 Elo** in games with 10+ minute time controls
- **+75-100 Elo** in 3-5 minute blitz games
- No change in bullet (already optimized for speed)

### Verification Commands

```bash
# Check bot status
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="sudo docker ps | grep c0br4"

# View bot logs
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="sudo docker logs -f c0br4-production-v31"

# Test engine directly
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="echo 'uci' | /home/c0br4/engines/C0BR4_v3.3/C0BR4_v3.3"

# Check engine version in container
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="sudo docker exec c0br4-production-v31 cat config.yml | grep 'C0BR4_v'"
```

### Monitoring

- **Lichess Profile**: https://lichess.org/@/c0br4_bot
- **Bot Status**: Currently online and accepting challenges
- **Log Location**: `/home/c0br4/logs/` on VM

### Deployment Process Used

1. Switched to correct GCP project: `c0br4-lichess-bot`
2. Built Linux x64 binary using `dotnet publish -c Release-Linux`
3. Uploaded engine to `/home/c0br4/engines/C0BR4_v3.3/`
4. Updated `config.yml` with v3.3 engine path and greeting
5. Made binary executable: `chmod +x`
6. Restarted Docker container: `docker restart c0br4-production-v31`
7. Verified engine responds correctly to UCI commands

### Configuration Changes

**Engine Path** (in config.yml):
```yaml
engine:
  dir: "./engines/C0BR4_v3.3/"
  name: "C0BR4_v3.3"
  protocol: "uci"
```

**Greeting Messages**:
```yaml
greeting:
  hello: "Hello! I'm c0br4_bot, running C0BR4 v3.3 with adaptive depth (6-10) on Google Cloud. Good luck!"
  goodbye: "GG. Thanks for playing c0br4_bot!"
  hello_spectators: "Welcome! You're watching c0br4_bot v3.3 with adaptive time management!"
  goodbye_spectators: "Thanks for watching c0br4_bot!"
```

### Next Steps

1. **Monitor Performance**: Watch games on Lichess to see adaptive depth in action
2. **Check Logs**: Monitor for any errors or unusual behavior
3. **Rating Tracking**: Track rating changes over next 20-30 games
4. **Time Management**: Verify no time trouble in longer games
5. **Depth Achievement**: Confirm depth 6+ in blitz, depth 10 in classical

### Rollback Plan (if needed)

To revert to v3.2:
```bash
# Update config to point to v3.2
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="sed -i 's/C0BR4_v3.3/C0BR4_v3.2/g' /home/c0br4/config.yml"

# Restart container
gcloud compute ssh c0br4-production-bot --zone=us-central1-a --project=c0br4-lichess-bot \
  --command="sudo docker restart c0br4-production-v31"
```

### Cost Estimate

- **VM (e2-small)**: ~$15-18/month (24/7)
- **Network**: ~$1-2/month
- **Storage**: ~$0.50/month
- **Total**: ~$16-21/month

### Success Criteria

- ✅ Bot is online and accepting challenges
- ✅ Engine v3.3 loaded and responding to UCI
- ✅ Configuration updated correctly
- ✅ Container running stably
- 🔄 Awaiting first game to verify adaptive depth

---

**Deployment completed**: November 27, 2025 at 03:10 UTC  
**Deployed by**: Automated deployment script  
**Engine version**: C0BR4 v3.3 (Adaptive Time Management & Depth Search)  
**Status**: ✅ **DEPLOYED AND RUNNING**
